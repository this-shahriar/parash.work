import { Flex } from "@chakra-ui/react";
import Container from "../../components/Container";

const Blog = () => {
  return (
    <Container>
      <Flex>this is the blog</Flex>
    </Container>
  );
};

export default Blog;
